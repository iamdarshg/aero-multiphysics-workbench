console.log("Platform development preflight only; no persistent server was started.");
console.log("Use `node mcp/engineering/server.ts` explicitly for a user-owned stdio MCP session.");
console.log("Local native solver runs require a READY result from scripts/platform/capabilities.mjs and scheduler admission.");
