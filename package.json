{
  "name": "aero-multiphysics-workbench",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "audit:requirements": "node scripts/audit-requirements.mjs",
    "dev": "pnpm --filter @aero/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test && node --test tests/smoke/workspace.test.mjs && uv run --directory services/api pytest -q",
    "typecheck": "pnpm -r typecheck && uv run --directory services/api mypy ../../packages/core aeroworkbench_api",
    "lint": "pnpm -r lint && uv run --directory services/api ruff check ../../packages/core aeroworkbench_api ../../tests/core",
    "verify:workspace": "powershell -NoProfile -File scripts/verify-workspace.ps1"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0"
  }
}
