# Platform benchmark tier

`node scripts/platform/benchmark.mjs` exercises contract, capability, scheduling, and MCP permission behavior. It is **not** a physics benchmark and must not be reported as numerical solver validation.

Native solver benchmarks belong here only with their obtained case input, solver version, mesh/time-step controls, expected tolerances, result provenance, and execution receipt.
